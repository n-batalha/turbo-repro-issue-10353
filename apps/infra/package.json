{
  "name": "infra",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "pnpm exec sst deploy",
    "install-sst-deps": "pnpm exec sst install",
    "sst-dev": "sst dev",
    "infra-local": "docker compose up",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.777.0",
    "@tsconfig/node20": "^20.1.4",
    "web": "workspace:*",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "typescript": "5.8.2"
  },
  "dependencies": {
    "sst": "3.13.9"
  },
  "engines": {
    "node": ">=20"
  }
}
